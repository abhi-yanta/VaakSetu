# Copy to .env and fill in locally. Never commit real keys.
#   Copy-Item .env.example .env

BHASHINI_USER_ID=
BHASHINI_UDYAT_KEY=
BHASHINI_INFERENCE_KEY=

# Optional — enables AI4Bharat Layer 1 TTS via HuggingFace
# (already supported via String.fromEnvironment / --dart-define)
HF_TOKEN=
