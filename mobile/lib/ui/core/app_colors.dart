import 'package:flutter/material.dart';

class AppColors {
  // Brand Indian Heritage Saffron & Teal
  static const Color primarySaffron = Color(0xFFE65100);
  static const Color primarySaffronLight = Color(0xFFEF6C00);
  static const Color deepTeal = Color(0xFF00695C);
  static const Color deepTealLight = Color(0xFF00897B);

  // Light rural-friendly surfaces (token names kept for minimal churn)
  static const Color bgDark = Color(0xFFF7F3EB);
  static const Color surfaceDark = Color(0xFFFFFFFF);
  static const Color surfaceDarkElevated = Color(0xFFEFEAE2);
  static const Color borderDark = Color(0xFFD4CEC4);

  // Safety Hierarchy (light tint backgrounds + vivid accents)
  static const Color safeGreen = Color(0xFF059669);
  static const Color safeGreenBg = Color(0xFFD1FAE5);

  static const Color warningYellow = Color(0xFFD97706);
  static const Color warningYellowBg = Color(0xFFFEF3C7);

  static const Color dangerRed = Color(0xFFDC2626);
  static const Color dangerRedBg = Color(0xFFFEE2E2);

  // Info / unrecognized (light-theme friendly)
  static const Color infoBlue = Color(0xFF0284C7);
  static const Color infoBlueBg = Color(0xFFE0F2FE);

  // Typography — dark ink on light surfaces
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF64748B);
}
